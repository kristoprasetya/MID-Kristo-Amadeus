#include<stdio.h>
#include<string.h>
#include<stdlib.h>

struct Node {
  int a,b;
  int number;
  char name[255];
  int date;
  int year;
  char month[255];
};

Data *createData (char *name, char *month, int date, int number, int year){
  Data *newData = (Data*)malloc(sizeof(Data));
  strcpy(newData->name, name);
  newData->number;
  newData->date;
  newData->year;
  newData->month;
}

int main(){
  scanf("%d %d", &a, &b);
  scanf("&%d &%c &%d - &%c &%c", date, month, year, name name);
  scanf("&%d &%c &%d - &%c &%c", date, month, year, name name);
  scanf("&%d &%c &%d - &%c &%c", date, month, year, name name);
  scanf("&%d &%c &%d - &%c &%c", date, month, year, name name);
  scanf("&%d &%c &%d - &%c &%c", date, month, year, name name);
  scanf("&%d &%c &%d - &%c &%c", date, month, year, name name);
  scanf("&%d &%c &%d - &%c &%c", date, month, year, name name);
  printf("Need 2 more cute");
  printf("%d %c %d - %c %c", date monthm year, name, name);
  printf("%d %c %d - %c %c", date monthm year, name, name);
}

